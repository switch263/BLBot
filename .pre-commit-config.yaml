# Correctness lint before every commit. Kept to ruff's pyflakes/E9 rules
# (configured in pyproject.toml) — no formatting hooks, so no repo-wide churn.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff

# Direct dependencies only — pip resolves the transitive tree (aiohttp's
# friends, etc.) itself. Compatible-release pins (~=) take bugfixes but not
# breaking majors on rebuild.
discord.py~=2.4
aiohttp~=3.11
Pillow~=10.1
lenny~=0.1.3
